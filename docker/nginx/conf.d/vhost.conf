server {
    set $base_root /app/web;
    root   $base_root;
    listen       80;
    server_name  localhost 127.0.0.1 test.test sol.ru;

    location / {
        root $base_root/;
        try_files $uri $uri /index.php$is_args$args;
    }

    location ~ \.php$ {
        fastcgi_pass   php:9000;
        fastcgi_index  index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

#upstream php {
#    server app:9000;
#}
#upstream php-fpm {
#    server backend:9000;
#}#upstream php {
  #    server app:9000;
  #}